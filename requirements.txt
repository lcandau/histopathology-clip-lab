# Pinned versions from the Colab T4 runtime used for the baseline.

tensorflow==2.20.0
tensorflow-text==2.20.1
keras==3.14.0
keras-hub==0.28.0

numpy==2.0.2
pandas==2.2.2
matplotlib==3.10.0
Pillow==11.3.0
scikit-learn==1.6.1

umap-learn==0.5.12
kagglehub==1.0.0

# Optional deps (installed per-notebook when needed):
#   PLIP zero-shot:  transformers, torch
#   Stain norm:      staintools
#   DINOv2:          torch, torchvision
